# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /data: mri_calgary_campinas
  - override /model: mri_calgary_campinas_bayes
  - override /callbacks: default
  - override /trainer: default
  - override /logger: wandb

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["mri_recon", "cascade"]

seed: 12345


logger:
  wandb:
    project: 'mri_recon_bayes'
    tags: ${tags}
    group: "mri_recon"
  aim:
    experiment: "mri_recon"

trainer:
  max_epochs: 20

data:
  data_dir: "/home/amirsha/scratch/GBM_norm"
  metadata_train_dir: "/lustre06/project/6054398/amirsha/MRI_REC_template/projects/uncertainty/csvs/train_gbm_chal.csv"
  metadata_val_dir: "/lustre06/project/6054398/amirsha/MRI_REC_template/projects/uncertainty/csvs/train_gbm_chal.csv"
  metadata_test_dir: "/lustre06/project/6054398/amirsha/MRI_REC_template/projects/uncertainty/csvs/train_gbm_chal.csv"
  num_slices: 152

paths:
  log_dir: "/lustre07/scratch/amirsha/logs/"